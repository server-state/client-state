{
  "name": "@server-state/client-base",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.8.3",
    "@material-ui/icons": "^4.5.1",
    "@material-ui/styles": "latest",
    "@server-state/tablelike-table-cbm": "^0.1.6",
    "axios": "^0.19.1",
    "prop-types": "^15.7.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "cypress:open": "cypress open",
    "serve:build": "http-server build -d -e --cause -p 3479",
    "ci:e2e": "start-server-and-test serve:build http://localhost:3479 cy:run",
    "cy:run": "cypress run --record"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cypress": "^3.8.2",
    "eslint-plugin-cypress": "^2.8.1",
    "http-server": "^0.12.1",
    "start-server-and-test": "^1.10.6"
  }
}
